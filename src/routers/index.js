const root = require("./root");
const auth = require("./auth");
const admin = require("./admin");
const cart = require("./cart");
const order = require("./order");

module.exports = { root, auth, admin, cart, order };